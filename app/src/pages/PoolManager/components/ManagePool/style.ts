import styled from "styled-components";

export const NoPool = styled.div`
    text-align: center;
    font-weight: bold;
`

export const PoolManagerContainer = styled.div`
    display: flex;
    flex-direction: column;
`
